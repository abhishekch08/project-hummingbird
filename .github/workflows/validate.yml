name: Public repository checks

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  public-regression:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v7
      - name: Run structural, requirements and scenario checks
        run: python3 scripts/verification/check_all.py
